# Optionnel : force l'hôte des liens ICS générés.
# Non défini, l'app suit le domaine de la requête (utile en multi-domaine).
#HOST=https://exemple.tld
SALT=your_salt
SINCE_MONTHS=6
UNTIL_MONTHS=12
CALDAV_URL_PLACEHOLDER=your_caldav_url
# dev only
DEFAULT_USER=
DEFAULT_PASSWORD=
DEFAULT_CALDAV_URL=